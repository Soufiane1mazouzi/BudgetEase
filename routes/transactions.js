const express = require('express');
const router = express.Router();

// Define CRUD routes for transactions
router.post('/', (req, res) => {
  // Create transaction
});

router.get('/', (req, res) => {
  // Get all transactions
});

module.exports = router;
